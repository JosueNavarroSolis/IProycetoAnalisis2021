package i_proyecto_aa_2021;

/**
 * Enum that contains the different states a suggestion can be
 * in when checking for marked cards.
 * @author Hans
 */
public enum MarkedState {
    NONE, BEFORE_INDEX, ON_INDEX, AFTER_INDEX, ON_AFTER_INDEX
}
